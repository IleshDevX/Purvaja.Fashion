$kind: http-request
url: "{{baseUrl}}/api/v1/cart/items"
method: POST
headers:
  Content-Type: application/json
  X-CSRF-Token: "{{csrfToken}}"
body:
  type: text
  content: |-
    {
      "variantId": "{{variantId}}",
      "quantity": 1
    }
order: 2000
