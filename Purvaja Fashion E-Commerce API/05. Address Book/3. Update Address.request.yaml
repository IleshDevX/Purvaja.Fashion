$kind: http-request
url: "{{baseUrl}}/api/v1/addresses/{{addressId}}"
method: PATCH
headers:
  Content-Type: application/json
  X-CSRF-Token: "{{csrfToken}}"
body:
  type: text
  content: |-
    {
      "isDefault": true
    }
order: 3000
