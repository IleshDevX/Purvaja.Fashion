$kind: http-request
url: "{{baseUrl}}/api/v1/admin/orders/{{orderId}}/status"
method: PATCH
headers:
  Content-Type: application/json
  X-CSRF-Token: "{{csrfToken}}"
body:
  type: text
  content: |-
    {
      "status": "PROCESSING"
    }
order: 6000
