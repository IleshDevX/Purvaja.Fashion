$kind: http-request
url: "{{baseUrl}}/api/v1/orders/{{orderId}}/cancel"
method: POST
headers:
  Content-Type: application/json
  X-CSRF-Token: "{{csrfToken}}"
body:
  type: text
  content: |-
    {
      "reason": "Changed mind before dispatch"
    }
order: 8000
