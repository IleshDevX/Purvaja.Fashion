$kind: http-request
url: "{{baseUrl}}/api/v1/orders/{{orderId}}/returns"
method: POST
headers:
  Content-Type: application/json
  X-CSRF-Token: "{{csrfToken}}"
body:
  type: text
  content: |-
    {
      "reason": "Size does not fit as expected"
    }
order: 9000
